import { Inter, Roboto_Slab } from 'next/font/google';

export const inter = Inter({ subsets: ['latin-ext'], variable: '--font-primary' });
export const robotoSlab = Roboto_Slab({ subsets: ['latin-ext'], variable: '--font-secondary' });
